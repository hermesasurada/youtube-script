# youtube-script 런타임 의존성
# 설치: .venv/bin/pip install -r requirements.txt
#
# 외부 바이너리(별도 설치 필요): whisper.cpp(whisper-cli), ffmpeg/ffprobe, claude CLI

Flask==3.1.3
yt-dlp==2026.3.17
python-dotenv==1.2.2

# 테스트용 (런타임 불필요): .venv/bin/python -m pytest
pytest==9.0.3
