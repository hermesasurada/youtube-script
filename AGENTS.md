# youtube-script project decisions

- 기관·프로젝트·임무명도 낯선 영문 약어이면 첫 등장 단락 뒤에 풀네임과 짧은 각주를 붙인다. 본문에 역할 설명이 있거나 전사의 음차를 영문으로 교정한 경우에도 생략하지 않는다(CADRE 사례). 풀네임 추측 금지·명시적 제외 목록 우선·용어당 1회 규칙은 유지한다.

- 이미 발행된 요약의 버튼은 `발행됨`이며, 누르면 `블로그에서 보기`/`지금 내용으로 수정` 메뉴(`YS.openBlogMenu`)를 띄운다. 메뉴 머리에는 게시 시각(`blog_published_at`, 분 단위)을 두고 수정 항목에는 마지막 수정 시각을 둬 바로 견줄 수 있게 한다(버튼 tooltip에도 게시 시각). 수정은 발행 이후 내용이 바뀌었거나(요약 파일 mtime·`summary_notes.updated_at`) 발행 당시 렌더 버전(`items.blog_render_ver`, DB v20)이 현재 `app.BLOG_RENDER_VERSION`과 다를 때 활성화되고(`_blog_state`, 1초 여유), 서버는 `/history/publish-blog`에 `mode="update"`로 기존 `blog_post_id`의 제목·본문을 덮어쓴다. URL은 유지되고 `blog_published_at`만 갱신돼 판정이 초기화된다. 발행시각 기록이 없는 옛 글은 수정 가능으로 본다.
- 수동 큐 등록은 같은 채널을 **지난번에 넣은 캡처·증류 설정**을 기본값으로 따른다(`db.last_queue_prefs_for_channel`). 캡처는 그 채널 최근 큐 항목의 명시값, 증류는 `items.distill`(요약 화면에서 바꾼 최종 상태)을 먼저 보고 없으면 큐 값, 그래도 없으면 모니터 채널 기본(`channels.capture/distill`) 순이다. `/queue/preview`가 `prefs`로 내려주면 추가 폼의 칩이 그대로 맞춰지고 근거 문구를 보여 준다(그 자리에서 바꿔도 된다). 폼을 거치지 않는 호출을 위해 `/queue/items` POST도 값이 없으면 서버에서 같은 규칙으로 채운다. 이력이 없으면 종전처럼 둘 다 켜짐.

- 자동 모니터의 중복 정리(`이미 처리된 이력 확인`)는 **요약까지 끝난 이력**에만 적용한다(`db.has_summary_for_yt_id`). 요약 SSE가 `MONITOR_SUMM_TIMEOUT`(기본 900초)을 넘겨 끊기면 전사 파일만 남는데, 그 반쪽 이력을 중복으로 닫으면 요약이 영영 생기지 않는다(2026-09-13 Qualcomm 47분 영상). 긴 영상에서 반복되면 이 env로 타임아웃을 올린다.

- 요약 뷰어의 `###` 소제목 옆에는 ✎ 편집 버튼이 있어 그 구간의 **마크다운을 직접 고친다**. 뷰어가 화면의 마크다운을 `###` 단위로 잘라(`YS.splitSummarySections`, 코드펜스 안의 `###`은 제외) 고친 구간의 원래 텍스트와 새 텍스트를 `/summary/section`(원격 허용)에 함께 보내고, 서버는 원래 텍스트가 파일에 **정확히 한 번** 나올 때만 갈아 끼운 뒤 재색인한다(아니면 409 — 요약 재생성·다른 탭 편집 감지). 저장 후 본문을 다시 그리며 각주·메모·sticky도 다시 붙인다. 편집 버튼과 편집창은 블로그 발행 HTML에서 제거한다.

- 섹션별 사용자 메모는 `summary_notes` 테이블(DB v19)에 전사 경로·소제목·동명 소제목 순번 기준으로 저장한다. 재색인 시 유지하며 일반·몰입형·모바일 요약에서 편집할 수 있다. 표시는 머리말 없이 본문만 두고 첫 글자 앞에 💡를 붙인다(화면은 `.summary-memo-text::before`, 인라인 style만 먹는 블로그 본문은 문자 자체를 앞에 넣는다). 새 블로그 발행에는 서버의 최신 저장 메모를 이스케이프한 텍스트와 인라인 스타일로 포함한다. 빈 내용 저장은 해당 메모 삭제이며, 행은 `body=''`로 남겨 '지웠다'는 시각을 보존한다(행을 지우면 발행 뒤 삭제한 경우를 '변경 없음'으로 잘못 판정한다). 메모 저장·삭제는 `/summary/note` 응답의 `blog` 상태와 `ys:blog-state` 이벤트로 열려 있는 발행 버튼에 즉시 반영된다. 이미 발행된 글의 자동 갱신은 하지 않는다. **블로그 본문 표시 형식을 바꾸면(각주 배치, 메모 모양 등) `BLOG_RENDER_VERSION`을 올리고 서버를 재기동할 것** — 올리지 않으면 이미 발행된 글이 옛 모양으로 남고 수정 버튼도 비활성이라 되돌릴 방법이 없다(2026-09-14 실제 발생).

- 요약의 `## 3. 핵심 내용`에서 일반 독자에게 생소한 약어·기관·시장/기술 용어는 첫 등장에만 `\*`를 표시하고 정의만 담은 짧은 한 문장 해설을 둔다. Codex·ASIC·HBM·open-weight(오픈웨이트)·FSD·ETF·FDA·AGI·Neocloud·bay(베이)·토큰(token)·API·LLM·GPU·CPU·SaaS·클라우드·데이터센터·반도체·오픈소스·스타트업·벤치마크·프롬프트·에이전트·강화학습·휴머노이드·피지컬 AI·샌드박스·KV 캐시·MoE·RAG·어텐션·chain of thought(CoT)·컨텍스트 창은 알려진 용어로 간주해 별표와 해설을 붙이지 않는다. Neocloud는 `뉴클라우드`·`네오클라우드`로 음차하지 않고 제목과 본문 모두 원어로 표기한다. 영문 단어·용어의 각주 제목은 원어만 쓰고 한글 음차를 병기하지 않는다(본문에서 플라이휠·임베딩 같은 음차 외래어나 비추력 같은 번역어로 썼어도 영문 원어가 있으면 제목은 원어만 — 화면 `common.js`가 `한글 (English)` 제목을 영어만 남기므로 프롬프트도 같은 기준이어야 한다). 각주 개수 상한은 두지 않는다. 본문에 풀네임·역할이 나와도 이름만으로 무엇인지 알 수 없으면 해설한다 — 각주는 풀네임 반복이 아니라 '무엇인지'를 알려 주는 자리다. 각주를 단 용어에는 반드시 `\*`를 붙여 별표와 각주를 짝지운다(2026-09-26 사용자 지시). 영문 약어는 확실히 특정되는 표준 풀네임을 `약어 (Full Name)` 형식으로 함께 쓰되 추측하지 않는다. 본문 반복·추가 배경·함의는 넣지 않으며, 같은 용어를 반복 설명하거나 회사·제품명과 일반어까지 남발하지 않는다.
- `## 2. 한눈 요약`은 각주 대상이 아니다 — 별표도 `term-notes`도 넣지 않는다(프롬프트 규칙 + 렌더의 `_stripBriefMarks`가 이중 보장). 설명이 필요한 용어는 `## 3. 핵심 내용` 첫 등장에서만 각주한다.
- 제외 용어의 본문 별표는 `_stripExcludedMarks`가 지운다. 본문 표기가 `증류(distillation)*`처럼 원어 병기라 닫는 괄호를 허용하고, 볼드로 노드가 갈리는 경우(`**정렬(alignment)**\*`)까지 잡으려고 블록 전체 텍스트에서 위치를 찾아 그 별표가 든 노드만 고친다.
- 요약 뷰어의 각주 행에는 ✕ 버튼이 있어 그 용어를 `term_exclusions`(DB v18)에 넣는다. 제외 목록은 화면 렌더(`applyTermExclusions`)·블로그 발행 HTML·새 요약 프롬프트(`_inject_term_exclusions`, 전사 자리표시자 앞 한 줄)에 모두 적용되며, 프롬프트 패널 하단에서 칩으로 보고 지운다. API는 `/terms/excluded` GET·POST·DELETE(원격 허용).
- 구성 요소가 모두 일상 한국어 단어여서 결합만으로 뜻이 드러나는 합성어(제품-시장 적합성, 언어 모델, 코딩 에이전트 등)는 각주 대상이 아니다. 한국어 단어로 이뤄졌어도 분야를 모르면 뜻을 알 수 없는 말(레티클 한계, 투기적 디코딩)은 각주 대상이다.
- 이미지 캡처의 캡션은 용어 각주 대상이 아니다. 캡션에는 별표를 붙이지 않으며, 비전 모델이 붙여 오더라도 주입 시점(`_caption_text`)과 화면 렌더(`.kf-strip figcaption`)에서 제거한다.
- 용어 해설은 용어가 처음 나온 단락 바로 다음에 `<div class="term-notes">`로 묶고, 서로 다른 단락의 각주를 섹션 끝으로 합치지 않으며, 용어마다 `<p class="term-note">…</p>` 한 행을 사용한다. 데스크톱·모바일 요약 화면과 블로그 발행 본문 모두 묶음 하나에만 인용선을 표시하고, 각주 행 사이는 가로선·추가 여백 없이 줄바꿈으로만 구분하며 본문보다 작은 회색 글꼴로 표시한다. 설명은 `~이다`·`~한다` 같은 완결문과 마침표를 쓰지 않고 간결한 명사형 문구로 끝낸다. 블로그에는 외부 CSS 대신 인라인 스타일로 보존한다.
- 요약 팝업 상단 메타정보 날짜는 `YY.MM.DD`로 표시하고, 영상 길이와 원문 대비 압축률은 하나의 칩으로 묶는다. 저장된 메타 원문은 바꾸지 않는다.
- 요약 팝업 상단 메타 칩은 기본 본문 글꼴을 사용하고 작은 패딩·간격으로 한 줄 우선 배치한다.
- 모바일 요약 헤더의 뒤로가기·YouTube·글꼴·글자크기 컨트롤은 높이 32px로 통일한다.
- 요약 팝업의 `###` 소제목은 제목 고정 영역 아래 8px 여백을 유지하며 해당 섹션 안에서만 sticky로 유지되며, 다음 `###` 섹션이 올라오면 이전 소제목이 밀려나야 한다. 저장 마크다운과 블로그 HTML에는 섹션 래퍼를 넣지 않는다.
- 채널 모니터 요약은 **슬롯 목록**(1~5개, 슬롯마다 구체 모델+추론 수준)을 큐 영상마다 한 칸씩 넘겨 시작 슬롯을 정하고, 실패하면 같은 순환의 다음 슬롯으로 폴백한다(2026-09-23 사용자 지시로 계열 3개 고정에서 확장). 같은 계열·같은 모델을 여러 슬롯에 둘 수 있다. 저장은 `monitor_settings.summary_slots`(DB v23 — 기존 계열 순번·버전·추론에서 옮겼다)와 `summary_cursor`, 원자적 예약은 `db.reserve_monitor_summary_slots`. 게이트(`summarizer_gate`)는 계열 단위라 슬롯 계열 순서로 묻고 쓸 수 있는 계열의 첫 슬롯부터 `/summarize`에 `slots`로 넘긴다. 제목 번역은 `TITLE_TR_MODEL`(GPT-6 Luna, 추론 기본값·플래그 없음, 2026-09-23 사용자 지시)이 먼저 맡고, 실패한 묶음만 요약 슬롯 순번(각 슬롯 모델, 추론 기본값)으로 넘긴다. 수동 요약처럼 모델 하나만 필요한 경로는 `app._model_versions()`(계열별 첫 슬롯)를 쓴다.
- 채널 모니터의 모델 선택기는 **wm과 같은 디자인**이다(2026-09-23 사용자 지시로 통일·간소화). 화면은 `static/js/model-selector.js`·`static/css/model-selector.css`가 그리며, 두 파일은 wm의 `static/model-selector.js`·`static/model-selector.css`와 **글자 단위로 같아야 한다**(`tests/test_model_versions.py`가 대조). yt는 렌더러의 '슬롯 모드'(행마다 모델·추론, 빼기 ×, '+ 슬롯 추가 n/5')를 쓰고 wm은 '계열 모드'를 쓴다. 상태·저장은 `common.js`의 `YS.monitorModels`가 맡고 데스크톱·모바일이 같이 쓴다. 구성: 요약 슬롯, '예외 규칙'(`/channels`의 `model_notes`, 읽기 전용). 바꾸면 바로 저장한다. 목록 화면의 진입 버튼은 wm 설정 버튼과 같은 톱니 아이콘이다.
- 모델 목록은 `llm_gateway.SUMMARY_MODEL_CHOICES`(Opus 최신 별칭·Opus 5.5·GPT 6 Astra/Sol/Luna·Grok CLI 기본) 하나를 쓴다. `opus` 별칭은 Claude CLI의 최신 Opus로 풀린다(2026-09-23 현재 `claude-opus-5-5`). **캡처 전용 모델 설정은 없다**(2026-09-23 사용자 지시로 삭제) — `/keyframes`가 순서를 받지 않으면 `app._capture_order`가 요약 파일의 `SUMMARY_MODEL` 마커로 그 영상을 요약한 모델을 찾아 먼저 쓰고, 실패하면 요약 슬롯 순서(중복 제거, vision 가능 모델만)로 넘긴다. 자동모니터·수동 캡처·재요약 배치가 모두 이 경로다. `monitor_settings.capture_models` 컬럼은 남아 있지만 읽지 않는다. **캡처에는 추론 수준을 두지 않는다** — 같은 프레임 8장을 GPT 6 Astra low/high로 판정해 유지 8/8·소제목 8/8이 같고 캡션 5/8이 글자까지 같았으며 시간만 15.0→16.8초로 늘었다(2026-09-23).
- 외국어 영상 제목을 `title_ko`로 번역해 Blogger에 발행할 때 글 제목은 번역 제목을 사용하고, 본문 첫 줄에는 `원제 : {원래 제목}`을 한 번만 표시한다. 한국어 원제이거나 번역 제목과 원제가 같으면 원제 행을 만들지 않으며, 서버 발행 경로가 이 규칙을 최종 보장한다.
- 한국어로 번역·재게시한 외부 영상(비즈카페 등)은 **그 영상을 그대로 전사·요약**하고, 공개된 YouTube 제목·설명문의 명시적 출처 링크를 우선하되 없으면 공개 메타에서 영문 검색어를 만들어 YouTube 후보의 제목·길이·업로더를 대조해 원본을 찾아 **링크만** 단다(전사 frontmatter `original_video_*`, 요약 화면에서 `번역본`/`원본` 링크). 로컬 전사문은 원본 검색용 LLM 호출에 보내지 않고, 탐색 실패는 전사·요약을 막지 않는다. **원본으로 갈아타 전사하는 방식은 2026-09-15에 시도했다가 원복했다** — 검색 기반 원본 후보가 실행마다 달라(같은 영상 3회에 후보 3개, 2개는 엉뚱한 영상) 엉뚱한 영상을 전사할 위험이 커서 사용자 지시로 되돌렸다. 다시 하려면 설명문의 명시적 출처(explicit)만 믿는 식으로 범위를 좁힐 것.

- 요약 모델 라벨은 **Claude 접두어 없이 등급+버전**으로 쓴다: `Opus 5.5`·`Opus 5`·`Grok 4.7`·`GPT 6 Astra`(2026-09-23 사용자 지시 — 한때 `Claude Opus 5.5`로 붙였다가 떼라고 함). `app._model_label`이 정본이며, 두 조각 ID(`claude-opus-5`)도 `Opus 5`로 바꾼다(예전엔 원문 그대로 찍혔다). 별칭(`opus`)만 알 때는 실제 응답으로 확인된 마지막 버전을, 그 전에는 `Opus`를 쓴다. 옛 요약 파일의 `claude-opus-5`·`Claude Opus 4.8`은 **파일을 고치지 않고** 화면에서 `YS.normalizeModelLabel`로 바꿔 보인다 — 요약 파일을 다시 쓰면 mtime이 바뀌어 발행된 블로그 글이 전부 '수정됨'이 된다. 모델 칩은 블로그 본문에도 들어가므로 표기 규칙을 바꾸면 `BLOG_RENDER_VERSION`도 올린다.

## 요약 문자 누출 게이트 (2026-09-12, 영어 조각 추가 2026-09-16)
- `_latin_fragments`: 한국어 문장에 남은 영어 조각도 누출로 본다. 소문자 4자 이상 영어 낱말이 ① 앞에 공백 두 칸(흘린 자리: ` eventual 수주`) ② 한글 낱말 안(`에피soode`) ③ 한글 조사가 바로 붙음(`concuss된`) 중 하나이고, 원문 전사·제목에 어간이 없고(`_stem`), 요약 안에 3회 미만일 때. 영문 제목·메타 표·캡션·각주·괄호 병기·코드·수식은 검사에서 뺀다. 띄어 쓴 일반 영어 용어(dense attention)는 두어 기술 용어를 억지로 옮기게 하지 않는다. 규칙을 바꾸면 기존 요약 전체로 오탐 재보정(2026-09-16 기준 1,112건 중 2.5% 검출, 대부분 실제 누출).
- `_GROK_STYLE_NOTE`: Grok 호출에만 프롬프트 맨 앞에 붙는 문체 보정(한국어 종결어미·한 문장 한 동작·주어 생략 금지·영어 조각 금지·표기 통일·불릿 1~2문장). Opus·Grok 5건씩 비교에서 정확도는 같고 Grok만 문장 마감이 흔들려 넣었다. 다른 모델 경로에는 붙이지 않는다.
- `app._script_leaks(text)`: 한국어 요약에 섞인 중국어 단어(主张하는·lateral移動)와 한글 사이 한 글자 한자(민粹주의), 아랍·키릴 문자를 찾는다. 한글 뒤 괄호 병기(반(反), 톈궁(天工, Tiangong))와 한 글자 단독 사용(前 CEO·對중국·전년比)은 정상으로 본다.
- `_summarize_ordered`의 GPT/Grok 경로는 누출이 있으면 같은 모델에 교정 지시(`_SCRIPT_RETRY_NOTE`)를 붙여 1회 재요청하고, 그래도 남으면 실패로 처리해 다음 모델로 넘긴다. Claude 스트리밍 경로는 `_warn_script_mix` 경고 로그만 남긴다.
- 규칙을 바꾸면 기존 요약 전체(`SUMMARY_DIR`)로 오탐을 다시 보정할 것(2026-09-12 기준 1,061건 중 오탐 0).
- whisper 반복 붕괴 판정(`_looks_collapsed`)은 같은 문장의 **연속 횟수(50회)** 와 **연속 시간(120초, `_COLLAPSE_MIN_SECS`)** 두 기준 중 하나면 붕괴다. 세그먼트가 30초씩 길게 나오면 10분이 27회에 불과해 횟수만으로는 못 잡는다(2026-09-15 CNBC 12분 영상: 654초 연속 → 컨텍스트 제한 재전사로 정상화). 붕괴가 그대로 저장되면 파싱 단계의 중복 제거 때문에 본문이 몇 줄로 줄어든 채 '정상 완료'로 보인다.
- 전사 번역(`transcript_translator`)은 청크마다 두 가드를 거친다(2026-09-26 사용자 지시). ① **줄 반복 루프**: 같은 긴 줄(15자+, 타임스탬프 제외)이 5회 이상이면 분량 기준(2.5배) 아래여도 폭주로 보고 재시도하며, 원문 자체가 반복(3회+)이면 따라간 것이라 폭주가 아니다. 재시도도 루프면 반복 적은 쪽을 고르고 연속 반복 줄을 접는다. ② **중국어·가나 누출**(`han_leaks`, 원문에 있던 조각·한글 뒤 괄호 병기·한글 사이에 끼지 않은 한 글자는 정상): 섞인 줄만 모아 한 번 교정(`purpose=repair`)하고, 교정본은 한자가 없고 **감지한 한자 구간과 그 뒤에 붙은 한글 어미·한글 괄호 풀이, 경계 띄어쓰기 밖이 한 글자도 바뀌지 않았을 때만** 받는다(`_only_leaks_replaced`, Astra 검토). 앞말은 넓히지 않고(10억 달러增加 → 엔으로 바꿔치기 차단, 가나만 예외: 세クター → 섹터), 대체어는 원래 붙어 있던 한글보다 한글이 늘어야 하며(구간 삭제 차단, 한글 사이 한 글자 잡음만 삭제 허용), 원래 구간에 없던 숫자·영문·문장부호를 들여오면 거부한다. 대체어의 뜻이 맞는지(증가→감소)는 코드로 판정하지 못한다. 운영 누출 줄 48개 실측: 옛 기준 44줄 채택, 새 기준은 틀린 교정·구간 밖 변경을 거르고 '整整'처럼 지우기만 한 교정도 거부(원래 줄 유지). 교정 호출이 실패해도 번역은 그대로 저장한다. 도입 시점 운영 번역본 5,096청크 중 1,287청크(25%)에 누출이 있었다.

## Shared LLM catalog

- 요약 선택기는 지원 실행기(Claude·Codex·Grok)의 중앙 카탈로그 text 모델을 조회하고, 캡처 순서는 그중 vision 가능 모델만 남긴다. Grok 구체 모델 ID도 허용하고 요약·제목 번역·캡처 호출에 전달한다. 비활성/미등록 기존 설정은 보존하되 신규 선택은 막는다. 요약 슬롯에 vision 모델이 하나도 없으면 `keyframe_report.VISION_MODEL`로 캡처한다.

- Model lists, display names and supported reasoning levels come from `~/projects/hermes-llm-log/llm_catalog.py` and `~/.hermes/data/llm_catalog.json`; manage them through the portal model-management page. Do not add independent UI option lists.
- Service selections, slot counts, routing and fallback order remain local. Preserve saved selections during catalog outages or model deactivation. New discoveries are candidates until manually enabled.
- Keep actual historical model IDs unchanged. Catalog readers must not depend on portal availability or trigger inference.
- 모델 선택기에는 '다음' 글자를 두지 않는다 — 다음 차례는 번호 배지 강조(`is-next`)만으로 표시한다(2026-09-23 사용자 지시).

- **중앙 LLM 카탈로그(`hermes-llm-log/llm_catalog.py`)는 부가 정보다 — 못 불러와도 본업이 멈추면 안 된다**(2026-09-23 사용자 지시로 수정). import는 반드시 `try/except Exception`으로 감싸고 실패하면 같은 API를 흉내 내는 `llm_catalog_fallback.py`(모르는 모델 허용·저장된 설정 유지)로 대신한다. 이 파일은 wm과 **같은 내용**으로 유지. 카탈로그 경로는 `sys.path.append`로 **뒤에** 붙여 이 저장소 모듈을 가리지 않게 한다. 대체 모드의 선택지는 무엇이든 '포함'으로 답하므로 **모델 계열 판단에 선택지 소속(`in …_CHOICES`)을 쓰지 말고** `llm_catalog.resolve()`의 provider → 모델 ID 접두어 순으로 판단한다. 검증은 문법 오류가 있는 `llm_catalog.py`를 PYTHONPATH 앞에 끼워 재현한다(`tests/test_catalog_isolation.py`).
- **회사·브랜드·제품·모델·기관명은 요약·제목 번역·전사 번역 모두 원문 표기**(NVIDIA, Google, Tesla, YouTube, NASA — 엔비디아·구글 같은 흔한 한글 표기도 쓰지 않는다, 2026-09-26 사용자 결정 A안). 인명도 원문 철자이고 예외는 일론 머스크뿐, 지명은 널리 알려진 곳만 한글. 세 프롬프트(`prompt.txt`, `_TITLE_TR_PROMPT`, `transcript_translator.SYSTEM_PROMPT`)를 같은 기준으로 유지하고, `notation.py`가 저장 직전에 흔한 한글 브랜드 표기를 원문으로 바꾸고 끝소리를 아는 영문 이름 뒤 조사(Anthropic가→Anthropic이, 볼드 `**Anthropic**가`도)를 고친다. 회사명 말고도 쓰이는 이름(아마존·애플·오라클·테슬라)은 조사가 붙어도 뜻이 갈려('아마존은 열대우림이다', '단위는 테슬라다') **자동 치환하지 않고** 모델에 맡긴다(Astra 재검토). 코드블록·여러 줄 인용·태그·링크 주소·확장자가 있는 경로(`…_x.frames/kf.jpg`)·H1·메타 표는 글 전체 기준으로 보호한다(줄 단위로 자르면 여러 줄 보호가 깨진다). 밑줄 볼드 `__X__`는 경로로 보지 않는다. 요약의 원문 제목(H1)·메타 표와 한국어 원제 영상의 제목은 건드리지 않는다. 전사 번역은 앞 청크에서 쓴 영문 고유명사 목록(최대 40개, 대문자 약어형 NVIDIA·xAI 포함, 흔한 약어 제외)을 다음 청크에 물려 표기를 잇되, 이번 원문에 더 분명한 철자가 있으면 원문을 우선하게 한다(첫 청크의 오표기가 끝까지 번지지 않게).
